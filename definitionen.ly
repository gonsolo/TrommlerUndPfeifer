% Kleine Trommel (k)

% Viertel (v)
kv = { \drummode { tommh4 } }

% Achtel (a)
ka = { \drummode { tommh8 } }

% Punktierte Achtel (ap)
kap = { \drummode { tommh8. } }

% Akzentuierte Achtel (ax)
kax = { \drummode { \ka \accent } }

% Sechzehntel (s)
ks = { \drummode { tommh16 } }

